[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Modo verboso por default — los tests de catálogos son pocos y queremos
# ver qué corre. Si la suite crece, considerar quitar `-v`.
addopts = -v --tb=short
